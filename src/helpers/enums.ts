
export enum UserRoles {
	Guest = "guest",
	Editor = "editor",
	Admin = "admin",
	SuperAdmin = "superAdmin"
}

export enum Authentication {
	Password = "password",
	Google = "google"
}

export enum PhotoTag {
	boring = "boring",
	nice = "nice",
	top = "top",
}